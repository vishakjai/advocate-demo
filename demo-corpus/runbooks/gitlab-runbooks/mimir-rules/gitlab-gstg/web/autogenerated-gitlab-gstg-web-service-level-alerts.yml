# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/service-component-alerts.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: 'Service Component Alerts: web'
  interval: 1m
  rules:
  - alert: WebServiceDependencyProxyErrorSLOViolation
    for: 2m
    annotations:
      title: The dependency_proxy SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "827448762"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="dependency_proxy",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceDependencyProxyErrorSLOViolation
    for: 2m
    annotations:
      title: The dependency_proxy SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "827448762"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="dependency_proxy",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceDependencyProxyErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The dependency_proxy SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "827448762"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="dependency_proxy",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceDependencyProxyErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The dependency_proxy SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "827448762"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="dependency_proxy",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="dependency_proxy",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceDependencyProxyTrafficCessation
    for: 5m
    annotations:
      title: The dependency_proxy SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "907657550"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceDependencyProxyTrafficAbsent
    for: 30m
    annotations:
      title: The dependency_proxy SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "907657550"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceDependencyProxyTrafficCessationRegional
    for: 5m
    annotations:
      title: The dependency_proxy SLI of the web service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "907657550"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceDependencyProxyTrafficAbsentRegional
    for: 30m
    annotations:
      title: The dependency_proxy SLI of the web service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Measures calls to Dependency Proxy as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that pulling images from the container registry takes longer than expected or fails outright.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "907657550"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"Groups::DependencyProxyForContainersController.*",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: virtual_registry
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="dependency_proxy",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceGlobalSearchApdexSLOViolation
    for: 2m
    annotations:
      title: The global_search SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4181740019"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="global_search",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceGlobalSearchApdexSLOViolation
    for: 2m
    annotations:
      title: The global_search SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4181740019"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="global_search",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceGlobalSearchApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The global_search SLI of the web service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4181740019"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="global_search",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceGlobalSearchApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The global_search SLI of the web service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4181740019"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="global_search",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="global_search",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceGlobalSearchTrafficCessation
    for: 5m
    annotations:
      title: The global_search SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3070581511"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_global_search_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="global_search",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="global_search",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceGlobalSearchTrafficAbsent
    for: 30m
    annotations:
      title: The global_search SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3070581511"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_global_search_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="global_search",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="global_search",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceGlobalSearchTrafficCessationRegional
    for: 5m
    annotations:
      title: The global_search SLI of the web service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3070581511"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_global_search_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="global_search",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="global_search",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceGlobalSearchTrafficAbsentRegional
    for: 30m
    annotations:
      title: The global_search SLI of the web service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        The number of Global Search search requests meeting their duration target based on the 99.95th percentile of the search with the same parameters.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3070581511"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_global_search_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="global_search",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="global_search",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceGroupManagementErrorSLOViolation
    for: 2m
    annotations:
      title: The group_management SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1120578811"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:confidence:ratio_1h{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:confidence:ratio_5m{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="group_management",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceGroupManagementErrorSLOViolation
    for: 2m
    annotations:
      title: The group_management SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1120578811"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:confidence:ratio_6h{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:confidence:ratio_30m{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="group_management",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceGroupManagementErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The group_management SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1120578811"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:confidence:ratio_1h{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:confidence:ratio_5m{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="group_management",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceGroupManagementErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The group_management SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1120578811"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:confidence:ratio_6h{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:confidence:ratio_30m{component="group_management",confidence="98%",env="gstg",monitor="global",type="web"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="group_management",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceGroupManagementTrafficCessation
    for: 5m
    annotations:
      title: The group_management SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1850758606"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="group_management",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="group_management",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceGroupManagementTrafficAbsent
    for: 30m
    annotations:
      title: The group_management SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1850758606"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="group_management",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="group_management",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceGroupManagementTrafficCessationRegional
    for: 5m
    annotations:
      title: The group_management SLI of the web service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1850758606"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="group_management",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="group_management",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceGroupManagementTrafficAbsentRegional
    for: 30m
    annotations:
      title: The group_management SLI of the web service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Measures calls to the Groups web resource as the number of unsuccessful calls vs the number of total calls.

        An alert on this SLI may indicate that users are unable to modify or create groups via the web.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1850758606"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{endpoint_id=~"GroupsController#create|GroupsController#update",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: groups_and_projects
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="group_management",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="group_management",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceImagescalerApdexSLOViolation
    for: 2m
    annotations:
      title: The imagescaler SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2653912995"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_image_resize_duration_seconds_bucket{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="imagescaler",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceImagescalerApdexSLOViolation
    for: 2m
    annotations:
      title: The imagescaler SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2653912995"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_image_resize_duration_seconds_bucket{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="imagescaler",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceImagescalerApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The imagescaler SLI of the web service in region `{{ $labels.region }}`
        has an apdex violating SLO
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2653912995"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_image_resize_duration_seconds_bucket{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="imagescaler",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceImagescalerApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The imagescaler SLI of the web service in region `{{ $labels.region }}`
        has an apdex violating SLO
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2653912995"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_image_resize_duration_seconds_bucket{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="imagescaler",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="imagescaler",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceImagescalerTrafficCessation
    for: 5m
    annotations:
      title: The imagescaler SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2371921356"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_image_resize_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_component_ops:rate_30m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceImagescalerTrafficAbsent
    for: 30m
    annotations:
      title: The imagescaler SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2371921356"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_image_resize_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_component_ops:rate_5m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceImagescalerTrafficCessationRegional
    for: 5m
    annotations:
      title: The imagescaler SLI of the web service in region `{{ $labels.region }}`
        has not received any traffic in the past 30m
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2371921356"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_image_resize_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceImagescalerTrafficAbsentRegional
    for: 30m
    annotations:
      title: The imagescaler SLI of the web service in region `{{ $labels.region }}`
        has not reported any traffic in the past 30m
      description: |
        The imagescaler rescales images before sending them to clients. This allows faster transmission of images and faster rendering of web pages.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2371921356"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_image_resize_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: user_profile
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="imagescaler",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceLoadbalancerErrorSLOViolation
    for: 2m
    annotations:
      title: The loadbalancer SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3255520955"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="loadbalancer",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceLoadbalancerErrorSLOViolation
    for: 2m
    annotations:
      title: The loadbalancer SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3255520955"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="loadbalancer",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceLoadbalancerErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The loadbalancer SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3255520955"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="loadbalancer",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceLoadbalancerErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The loadbalancer SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3255520955"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="loadbalancer",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="loadbalancer",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceLoadbalancerTrafficCessation
    for: 5m
    annotations:
      title: The loadbalancer SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "138982496"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceLoadbalancerTrafficAbsent
    for: 30m
    annotations:
      title: The loadbalancer SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "138982496"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceLoadbalancerTrafficCessationRegional
    for: 5m
    annotations:
      title: The loadbalancer SLI of the web service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "138982496"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceLoadbalancerTrafficAbsentRegional
    for: 30m
    annotations:
      title: The loadbalancer SLI of the web service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Measures aggregated HTTP request traffic through the HAProxy. 5xx responses are considered to be failures.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "138982496"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="loadbalancer",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceLoginsErrorSLOViolation
    for: 2m
    annotations:
      title: The logins SLI of the web service (`{{ $labels.stage }}` stage) has an
        error rate violating SLO
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "828867947"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="logins",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.010000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="logins",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="logins",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceLoginsErrorSLOViolation
    for: 2m
    annotations:
      title: The logins SLI of the web service (`{{ $labels.stage }}` stage) has an
        error rate violating SLO
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "828867947"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="logins",env="gstg",monitor="global",type="web"}
          > (6 * 0.010000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="logins",env="gstg",monitor="global",type="web"}
          > (6 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="logins",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceLoginsErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The logins SLI of the web service in region `{{ $labels.region }}` has
        an error rate violating SLO
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "828867947"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="logins",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.010000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="logins",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="logins",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceLoginsErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The logins SLI of the web service in region `{{ $labels.region }}` has
        an error rate violating SLO
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "828867947"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="logins",env="gstg",monitor="global",type="web"}
          > (6 * 0.010000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="logins",env="gstg",monitor="global",type="web"}
          > (6 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="logins",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceLoginsTrafficCessation
    for: 5m
    annotations:
      title: The logins SLI of the web service (`{{ $labels.stage }}` stage) has not
        received any traffic in the past 30m
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3751778691"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{endpoint_id!~".*#failure",endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="logins",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="logins",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceLoginsTrafficAbsent
    for: 30m
    annotations:
      title: The logins SLI of the web service (`{{ $labels.stage }}` stage) has not
        reported any traffic in the past 30m
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3751778691"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{endpoint_id!~".*#failure",endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="logins",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="logins",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceLoginsTrafficCessationRegional
    for: 5m
    annotations:
      title: The logins SLI of the web service in region `{{ $labels.region }}` has
        not received any traffic in the past 30m
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3751778691"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{endpoint_id!~".*#failure",endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="logins",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="logins",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceLoginsTrafficAbsentRegional
    for: 30m
    annotations:
      title: The logins SLI of the web service in region `{{ $labels.region }}` has
        not reported any traffic in the past 30m
      description: |
        Measures Logins as the number of successful Logins vs the number of initated attempts.

        An alert on this SLI may indicate that users are unable to login to GitLab.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3751778691"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{endpoint_id!~".*#failure",endpoint_id=~".*OmniauthCallbacksController.*|SessionsController#create",environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: system_access
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="logins",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="logins",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceRailsMiddlewarePathTraversalApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2341958666"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsMiddlewarePathTraversalApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2341958666"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsMiddlewarePathTraversalApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service in region
        `{{ $labels.region }}` has an apdex violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2341958666"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsMiddlewarePathTraversalApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service in region
        `{{ $labels.region }}` has an apdex violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2341958666"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsMiddlewarePathTraversalErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "31403210"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",request_rejected="true",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001500)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001500)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsMiddlewarePathTraversalErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "31403210"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",request_rejected="true",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (6 * 0.001500)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (6 * 0.001500)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsMiddlewarePathTraversalErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service in region
        `{{ $labels.region }}` has an error rate violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "31403210"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",region="{{ $labels.region }}",request_rejected="true",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001500)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.001500)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsMiddlewarePathTraversalErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service in region
        `{{ $labels.region }}` has an error rate violating SLO
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "31403210"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",region="{{ $labels.region }}",request_rejected="true",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (6 * 0.001500)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}
          > (6 * 0.001500)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_middleware_path_traversal",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsMiddlewarePathTraversalTrafficCessation
    for: 5m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1014620402"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceRailsMiddlewarePathTraversalTrafficAbsent
    for: 30m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1014620402"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceRailsMiddlewarePathTraversalTrafficCessationRegional
    for: 5m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service in region
        `{{ $labels.region }}` has not received any traffic in the past 30m
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1014620402"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceRailsMiddlewarePathTraversalTrafficAbsentRegional
    for: 30m
    annotations:
      title: The rails_middleware_path_traversal SLI of the web service in region
        `{{ $labels.region }}` has not reported any traffic in the past 30m
      description: |
        An alert here may indicate that we're rejecting more than 0.015% of requests because of suspected path traversal or the middleware execution time apdex is below 90%. We either have a larger amount of path traversal attempts than usual or we are incorrectly rejecting valid web requests. Look for the `path traversal attempt detected` messages in the logs and validate if they could be legit or not.

        If the requests are validly being blocked, please block the requests in Cloudflare. If the requests should not be blocked, the rejection can be disabled using `/chatops gitlab run feature set false check_path_traversal_middleware_reject_requests`.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1014620402"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_path_traversal_check_request_apdex_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="rails_middleware_path_traversal",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceRailsQueueingApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_queueing SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:confidence:ratio_1h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.005000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:confidence:ratio_5m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_queueing",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsQueueingApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_queueing SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:confidence:ratio_6h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.005000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:confidence:ratio_30m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_queueing",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsQueueingApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_queueing SLI of the web service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:confidence:ratio_1h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.005000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:confidence:ratio_5m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_queueing",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsQueueingApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_queueing SLI of the web service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:confidence:ratio_6h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.005000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:confidence:ratio_30m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_queueing",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsQueueingTrafficCessation
    for: 5m
    annotations:
      title: The rails_queueing SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceRailsQueueingTrafficAbsent
    for: 30m
    annotations:
      title: The rails_queueing SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceRailsQueueingTrafficCessationRegional
    for: 5m
    annotations:
      title: The rails_queueing SLI of the web service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceRailsQueueingTrafficAbsentRegional
    for: 30m
    annotations:
      title: The rails_queueing SLI of the web service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceRailsRequestApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsRequestApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the web service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsRequestApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the web service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsRequestApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the web service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="rails_request",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsRequestErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="rails_request",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="rails_request",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsRequestErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the web service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="rails_request",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="rails_request",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsRequestErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="rails_request",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="rails_request",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceRailsRequestErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the web service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="rails_request",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="rails_request",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceRailsRequestTrafficCessation
    for: 5m
    annotations:
      title: The rails_request SLI of the web service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceRailsRequestTrafficAbsent
    for: 30m
    annotations:
      title: The rails_request SLI of the web service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceRailsRequestTrafficCessationRegional
    for: 5m
    annotations:
      title: The rails_request SLI of the web service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceRailsRequestTrafficAbsentRegional
    for: 30m
    annotations:
      title: The rails_request SLI of the web service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceWorkhorseApdexSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the web service (`{{ $labels.stage }}` stage) has
        an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-web",route!="^/([^/]+/){1,}[^/]+/uploads\\z",route!="^/-/health$",route!="^/-/(readiness|liveness)$",route!="^/([^/]+/){1,}[^/]+\\.git/git-receive-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/git-upload-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/info/refs\\z",route!="^/([^/]+/){1,}[^/]+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",route!="^/.+\\.git/git-receive-pack\\z",route!="^/.+\\.git/git-upload-pack\\z",route!="^/.+\\.git/info/refs\\z",route!="^/.+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceWorkhorseApdexSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the web service (`{{ $labels.stage }}` stage) has
        an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-web",route!="^/([^/]+/){1,}[^/]+/uploads\\z",route!="^/-/health$",route!="^/-/(readiness|liveness)$",route!="^/([^/]+/){1,}[^/]+\\.git/git-receive-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/git-upload-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/info/refs\\z",route!="^/([^/]+/){1,}[^/]+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",route!="^/.+\\.git/git-receive-pack\\z",route!="^/.+\\.git/git-upload-pack\\z",route!="^/.+\\.git/info/refs\\z",route!="^/.+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceWorkhorseApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the web service in region `{{ $labels.region }}`
        has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",route!="^/([^/]+/){1,}[^/]+/uploads\\z",route!="^/-/health$",route!="^/-/(readiness|liveness)$",route!="^/([^/]+/){1,}[^/]+\\.git/git-receive-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/git-upload-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/info/refs\\z",route!="^/([^/]+/){1,}[^/]+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",route!="^/.+\\.git/git-receive-pack\\z",route!="^/.+\\.git/git-upload-pack\\z",route!="^/.+\\.git/info/refs\\z",route!="^/.+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 14.4 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceWorkhorseApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the web service in region `{{ $labels.region }}`
        has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",route!="^/([^/]+/){1,}[^/]+/uploads\\z",route!="^/-/health$",route!="^/-/(readiness|liveness)$",route!="^/([^/]+/){1,}[^/]+\\.git/git-receive-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/git-upload-pack\\z",route!="^/([^/]+/){1,}[^/]+\\.git/info/refs\\z",route!="^/([^/]+/){1,}[^/]+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",route!="^/.+\\.git/git-receive-pack\\z",route!="^/.+\\.git/git-upload-pack\\z",route!="^/.+\\.git/info/refs\\z",route!="^/.+\\.git/gitlab-lfs/objects/([0-9a-f]{64})/([0-9]+)\\z",stage="{{ $labels.stage }}",type="web"}[5m])
          )
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="workhorse",env="gstg",monitor="global",type="web"}
          < (1 - 6 * 0.002000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceWorkhorseErrorSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the web service (`{{ $labels.stage }}` stage) has
        an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="workhorse",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="workhorse",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceWorkhorseErrorSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the web service (`{{ $labels.stage }}` stage) has
        an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="workhorse",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="workhorse",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceWorkhorseErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the web service in region `{{ $labels.region }}`
        has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="workhorse",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="workhorse",env="gstg",monitor="global",type="web"}
          > (14.4 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 1
      )
  - alert: WebServiceWorkhorseErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the web service in region `{{ $labels.region }}`
        has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="workhorse",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="workhorse",env="gstg",monitor="global",type="web"}
          > (6 * 0.000100)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="web"}) >= 0.16667
      )
  - alert: WebServiceWorkhorseTrafficCessation
    for: 5m
    annotations:
      title: The workhorse SLI of the web service (`{{ $labels.stage }}` stage) has
        not received any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_component_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceWorkhorseTrafficAbsent
    for: 30m
    annotations:
      title: The workhorse SLI of the web service (`{{ $labels.stage }}` stage) has
        not reported any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"}
  - alert: WebServiceWorkhorseTrafficCessationRegional
    for: 5m
    annotations:
      title: The workhorse SLI of the web service in region `{{ $labels.region }}`
        has not received any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"} offset 1h >= 0.16666666666666666
  - alert: WebServiceWorkhorseTrafficAbsentRegional
    for: 30m
    annotations:
      title: The workhorse SLI of the web service in region `{{ $labels.region }}`
        has not reported any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: web-main/web-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/web-main/web-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-web",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="web"}[5m])
        )
      runbook: web/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="web"}
