# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/service-component-alerts.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: 'Service Component Alerts: ci-jobs-api'
  interval: 1m
  rules:
  - alert: CiJobsApiServiceRailsQueueingApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:confidence:ratio_1h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:confidence:ratio_5m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_queueing",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceRailsQueueingApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:confidence:ratio_6h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:confidence:ratio_30m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_queueing",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceRailsQueueingApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:confidence:ratio_1h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:confidence:ratio_5m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_queueing",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceRailsQueueingApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4121506195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_rails_queue_duration_seconds_bucket{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      confidence: 98%
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:confidence:ratio_6h{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:confidence:ratio_30m{component="rails_queueing",confidence="98%",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_queueing",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceRailsQueueingTrafficCessation
    for: 5m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} == 0
      and
      gitlab_component_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h >= 0.16666666666666666
  - alert: CiJobsApiServiceRailsQueueingTrafficAbsent
    for: 30m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"}
  - alert: CiJobsApiServiceRailsQueueingTrafficCessationRegional
    for: 5m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h >= 0.16666666666666666
  - alert: CiJobsApiServiceRailsQueueingTrafficAbsentRegional
    for: 30m
    annotations:
      title: The rails_queueing SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Apdex for time workhorse spends waiting for a free Puma worker.  When this alerts, a noticeable proportion of requests are arriving a Puma when there are no free workers, and thus queue for processing until a worker is free.

        Such queuing adds latency to the request which will often be user visible.

        Typically other apdex alerts will also be out of spec at the same time; this SLI adds signal that queuing is part of the problem, rather than only being slow processing requests with spare capacity in Puma for other requests.

        It typically indicates higher use of the system than it has been provisioned to handle; it can be resolved by either adding Puma workers (pods and nodes) or by finding high RPS traffic that can be eliminated.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1904440154"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_rails_queue_duration_seconds_count{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="rails_queueing",env="gstg",monitor="global",stage="main",type="ci-jobs-api"}
  - alert: CiJobsApiServiceRailsRequestApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.010000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceRailsRequestApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.010000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceRailsRequestApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.010000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceRailsRequestApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.010000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceRailsRequestErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceRailsRequestErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceRailsRequestErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceRailsRequestErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_request",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceRailsRequestTrafficCessation
    for: 5m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service (`{{ $labels.stage }}`
        stage) has not received any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} == 0
      and
      gitlab_component_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h >= 0.16666666666666666
  - alert: CiJobsApiServiceRailsRequestTrafficAbsent
    for: 30m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service (`{{ $labels.stage }}`
        stage) has not reported any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"}
  - alert: CiJobsApiServiceRailsRequestTrafficCessationRegional
    for: 5m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h >= 0.16666666666666666
  - alert: CiJobsApiServiceRailsRequestTrafficAbsentRegional
    for: 30m
    annotations:
      title: The rails_request SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="rails_request",env="gstg",monitor="global",stage="main",type="ci-jobs-api"}
  - alert: CiJobsApiServiceWorkhorseApdexSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-api",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_5m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceWorkhorseApdexSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-api",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_apdex:ratio_30m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceWorkhorseApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-api",region="{{ $labels.region }}",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_5m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceWorkhorseApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{code!~"5..",environment="{{ $labels.environment }}",fetched_external_url!="true",job=~"gitlab-workhorse|gitlab-workhorse-api",region="{{ $labels.region }}",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
          )
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_apdex:ratio_30m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceWorkhorseErrorSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceWorkhorseErrorSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceWorkhorseErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",region="{{ $labels.region }}",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 1
      )
  - alert: CiJobsApiServiceWorkhorseErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{code=~"^5.*",environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",region="{{ $labels.region }}",route!="^/-/health$",route!="^/-/(readiness|liveness)$",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="workhorse",env="gstg",monitor="global",type="ci-jobs-api"}) >= 0.16667
      )
  - alert: CiJobsApiServiceWorkhorseTrafficCessation
    for: 5m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service (`{{ $labels.stage }}` stage)
        has not received any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} == 0
      and
      gitlab_component_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h >= 0.16666666666666666
  - alert: CiJobsApiServiceWorkhorseTrafficAbsent
    for: 30m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service (`{{ $labels.stage }}` stage)
        has not reported any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"}
  - alert: CiJobsApiServiceWorkhorseTrafficCessationRegional
    for: 5m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h >= 0.16666666666666666
  - alert: CiJobsApiServiceWorkhorseTrafficAbsentRegional
    for: 30m
    annotations:
      title: The workhorse SLI of the ci-jobs-api service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: ci-jobs-api-main/ci-jobs-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/ci-jobs-api-main/ci-jobs-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gstg
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{environment="{{ $labels.environment }}",job=~"gitlab-workhorse|gitlab-workhorse-api",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="ci-jobs-api"}[5m])
        )
      runbook: ci-jobs-api/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="workhorse",env="gstg",monitor="global",stage="main",type="ci-jobs-api"}
