# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/saturation.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: Saturation Rules (autogenerated)
  interval: 1m
  rules:
  - record: gitlab_component_saturation:ratio
    labels:
      component: cpu
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            1 - avg by (env,environment,shard,stage,tier,type) (
              rate(node_cpu_seconds_total{mode="idle", env="pre",type="redis-sidekiq"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_inodes
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            1 - (
              node_filesystem_files_free{fstype=~"(ext.|xfs)", env="pre",type="redis-sidekiq"}
              /
              node_filesystem_files{fstype=~"(ext.|xfs)", env="pre",type="redis-sidekiq"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_maximum_capacity
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max by (device,env,environment,fqdn,shard,stage,tier,type) (
              node_filesystem_size_bytes{fstype=~"ext.|xfs", env="pre",type="redis-sidekiq"} - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="pre",type="redis-sidekiq"}
              unless on (fqdn) node_disk_info{serial=~"lvm.*", env="pre",type="redis-sidekiq"}
            ) / (64*2^40)
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_space
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            (
              1 - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="pre",type="redis-sidekiq"} / node_filesystem_size_bytes{fstype=~"ext.|xfs", env="pre",type="redis-sidekiq"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: memory
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            instance:node_memory_utilization:ratio{env="pre",type="redis-sidekiq"} or instance:node_memory_utilisation:ratio{env="pre",type="redis-sidekiq"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: nf_conntrack_entries
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max_over_time(node_nf_conntrack_entries{env="pre",type="redis-sidekiq"}[1m])
            /
            node_nf_conntrack_entries_limit{env="pre",type="redis-sidekiq"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: node_schedstat_waiting
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            avg without (cpu) (rate(node_schedstat_waiting_seconds_total{env="pre",type="redis-sidekiq"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: open_fds
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            (
              process_open_fds{env="pre",type="redis-sidekiq"}
              /
              process_max_fds{env="pre",type="redis-sidekiq"}
            )
            or
            (
              ruby_file_descriptors{env="pre",type="redis-sidekiq"}
              /
              ruby_process_max_fds{env="pre",type="redis-sidekiq"}
            )
            or
            (
              node_filefd_allocated{env="pre",type="redis-sidekiq"}
              /
              node_filefd_maximum{env="pre",type="redis-sidekiq"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: redis_clients
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max_over_time(redis_connected_clients{env="pre",type="redis-sidekiq"}[1m])
            /
            redis_config_maxclients{env="pre",type="redis-sidekiq"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: redis_memory
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max by (env,environment,fqdn,instance,shard,stage,tier,type) (
              label_replace(redis_memory_used_rss_bytes{env="pre",type="redis-sidekiq"}, "memtype", "rss","","")
              or
              label_replace(redis_memory_used_bytes{env="pre",type="redis-sidekiq"}, "memtype", "used","","")
            )
            /
            avg by (env,environment,fqdn,instance,shard,stage,tier,type) (
              node_memory_MemTotal_bytes{env="pre",type="redis-sidekiq"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: redis_primary_cpu
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            sum by (env,environment,fqdn,instance,shard,stage,tier,type) (
              rate(
                namedprocess_namegroup_thread_cpu_seconds_total{env="pre",type="redis-sidekiq", groupname="redis-server", threadname="redis-server"}[5m])
            )
            and on (fqdn) redis_instance_info{role="master"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: redis_secondary_cpu
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            (
              rate(redis_cpu_user_seconds_total{env="pre",type="redis-sidekiq"}[5m])
              +
              rate(redis_cpu_sys_seconds_total{env="pre",type="redis-sidekiq"}[5m])
            )
            and on (instance) redis_instance_info{role!="master"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: redis_sentinel_clients
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max_over_time(gitlab_redis_sentinel_connected_clients{env="pre",type="redis-sidekiq"}[1m])
            /
            gitlab_redis_sentinel_maxclients{env="pre",type="redis-sidekiq"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: single_node_cpu
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (1 - rate(node_cpu_seconds_total{mode="idle", env="pre",type="redis-sidekiq"}[5m]))
            ,
            1)
        ,
        0)
      )
