# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/saturation.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: Saturation Rules (autogenerated)
  interval: 1m
  rules:
  - record: gitlab_component_saturation:ratio
    labels:
      component: ci_runners_open_fds
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            (
              process_open_fds{env="pre",type="ci-runners"}
              /
              process_max_fds{env="pre",type="ci-runners"}
            )
            or
            (
              ruby_file_descriptors{env="pre",type="ci-runners"}
              /
              ruby_process_max_fds{env="pre",type="ci-runners"}
            )
            or
            (
              node_filefd_allocated{env="pre",type="ci-runners"}
              /
              node_filefd_maximum{env="pre",type="ci-runners"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: cpu
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            1 - avg by (env,environment,shard,stage,tier,type) (
              rate(node_cpu_seconds_total{mode="idle", env="pre",type="ci-runners"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_inodes
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            1 - (
              node_filesystem_files_free{fstype=~"(ext.|xfs)", env="pre",type="ci-runners"}
              /
              node_filesystem_files{fstype=~"(ext.|xfs)", env="pre",type="ci-runners"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_maximum_capacity
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max by (device,env,environment,fqdn,shard,stage,tier,type) (
              node_filesystem_size_bytes{fstype=~"ext.|xfs", env="pre",type="ci-runners"} - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="pre",type="ci-runners"}
              unless on (fqdn) node_disk_info{serial=~"lvm.*", env="pre",type="ci-runners"}
            ) / (64*2^40)
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_space
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            (
              1 - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="pre",type="ci-runners"} / node_filesystem_size_bytes{fstype=~"ext.|xfs", env="pre",type="ci-runners"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: memory
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            instance:node_memory_utilization:ratio{env="pre",type="ci-runners"} or instance:node_memory_utilisation:ratio{env="pre",type="ci-runners"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: nf_conntrack_entries
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            max_over_time(node_nf_conntrack_entries{env="pre",type="ci-runners"}[1m])
            /
            node_nf_conntrack_entries_limit{env="pre",type="ci-runners"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: node_schedstat_waiting
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            avg without (cpu) (rate(node_schedstat_waiting_seconds_total{env="pre",type="ci-runners"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: private_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="private"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="private"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: saas_linux_large_amd64_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-large-amd64"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-large-amd64"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: saas_linux_medium_amd64_gpu_standard_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-medium-amd64-gpu-standard"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-medium-amd64-gpu-standard"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: saas_linux_medium_amd64_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-medium-amd64"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-medium-amd64"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: saas_linux_small_amd64_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-small-amd64"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-linux-small-amd64"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: saas_macos_medium_m1_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-macos-medium-m1"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="saas-macos-medium-m1"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: shared_runners_gitlab
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="shared-gitlab-org"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="shared-gitlab-org"} > 0
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: single_node_cpu
    expr: |
      max by(env,environment,shard,stage,tier,type) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (1 - rate(node_cpu_seconds_total{mode="idle", env="pre",type="ci-runners"}[5m]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: windows_shared_runners
      stage: main
      tier: runners
      type: ci-runners
    expr: |
      max by(env,environment,shard) (
        clamp_min(
          clamp_max(
            sum without(executor_stage, exported_stage, state) (
              max_over_time(gitlab_runner_jobs{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="windows-shared"}[1m])
            )
            /
            gitlab_runner_limit{job=~"runners-manager|scrapeConfig/monitoring/prometheus-agent-runner",shard="windows-shared"} > 0
            ,
            1)
        ,
        0)
      )
