# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/service-component-alerts.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: 'Service Component Alerts: consul'
  interval: 1m
  rules:
  - alert: ConsulServiceConsulErrorSLOViolation
    for: 2m
    annotations:
      title: The consul SLI of the consul service (`{{ $labels.stage }}` stage) has
        an error rate violating SLO
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "862567575"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(consul_client_rpc_failed{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="consul",env="gprd",monitor="global",type="consul"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_5m{component="consul",env="gprd",monitor="global",type="consul"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="consul",env="gprd",monitor="global",type="consul"}) >= 1
      )
  - alert: ConsulServiceConsulErrorSLOViolation
    for: 2m
    annotations:
      title: The consul SLI of the consul service (`{{ $labels.stage }}` stage) has
        an error rate violating SLO
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "862567575"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(consul_client_rpc_failed{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="consul",env="gprd",monitor="global",type="consul"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,component)
        (
          gitlab_component_errors:ratio_30m{component="consul",env="gprd",monitor="global",type="consul"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="consul",env="gprd",monitor="global",type="consul"}) >= 0.16667
      )
  - alert: ConsulServiceConsulErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The consul SLI of the consul service in region `{{ $labels.region }}`
        has an error rate violating SLO
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "862567575"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(consul_client_rpc_failed{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="consul",env="gprd",monitor="global",type="consul"}
          > (14.4 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_5m{component="consul",env="gprd",monitor="global",type="consul"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="consul",env="gprd",monitor="global",type="consul"}) >= 1
      )
  - alert: ConsulServiceConsulErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The consul SLI of the consul service in region `{{ $labels.region }}`
        has an error rate violating SLO
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "862567575"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(consul_client_rpc_failed{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="consul",env="gprd",monitor="global",type="consul"}
          > (6 * 0.001000)
        )
        and on (env,environment,tier,type,stage,region,component)
        (
          gitlab_regional_sli_errors:ratio_30m{component="consul",env="gprd",monitor="global",type="consul"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="consul",env="gprd",monitor="global",type="consul"}) >= 0.16667
      )
  - alert: ConsulServiceConsulTrafficCessation
    for: 5m
    annotations:
      title: The consul SLI of the consul service (`{{ $labels.stage }}` stage) has
        not received any traffic in the past 30m
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(consul_client_rpc{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_component_ops:rate_30m{component="consul",env="gprd",monitor="global",stage="main",type="consul"} == 0
      and
      gitlab_component_ops:rate_30m{component="consul",env="gprd",monitor="global",stage="main",type="consul"} offset 1h >= 0.16666666666666666
  - alert: ConsulServiceConsulTrafficAbsent
    for: 30m
    annotations:
      title: The consul SLI of the consul service (`{{ $labels.stage }}` stage) has
        not reported any traffic in the past 30m
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(consul_client_rpc{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_component_ops:rate_5m{component="consul",env="gprd",monitor="global",stage="main",type="consul"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="consul",env="gprd",monitor="global",stage="main",type="consul"}
  - alert: ConsulServiceConsulTrafficCessationRegional
    for: 5m
    annotations:
      title: The consul SLI of the consul service in region `{{ $labels.region }}`
        has not received any traffic in the past 30m
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(consul_client_rpc{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="consul",env="gprd",monitor="global",stage="main",type="consul"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="consul",env="gprd",monitor="global",stage="main",type="consul"} offset 1h >= 0.16666666666666666
  - alert: ConsulServiceConsulTrafficAbsentRegional
    for: 30m
    annotations:
      title: The consul SLI of the consul service in region `{{ $labels.region }}`
        has not reported any traffic in the past 30m
      description: |
        Increments whenever a Consul agent in client mode makes an RPC request to a Consul server

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_datasource_id: mimir-gitlab-gprd
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(consul_client_rpc{environment="{{ $labels.environment }}",job="consul-gl-consul-ui",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: consul/#alerts
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="consul",env="gprd",monitor="global",stage="main",type="consul"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="consul",env="gprd",monitor="global",stage="main",type="consul"}
